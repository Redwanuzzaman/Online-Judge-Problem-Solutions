#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int cs = 1, t, a, b;
    char c, c1, c2;
    cin >> t;
    while(t--)
    {
        cin >> a >> c1 >> c >> c2 >> b;
        cout << a << b;
    }
}
