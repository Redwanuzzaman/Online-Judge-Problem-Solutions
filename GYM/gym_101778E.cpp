#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, x, y, dis, coin;
    cin >> t;
    while(t--)
    {
        cin >> n >> x >> y;
        pair <int, pair <int, int> > p;
        for(int i = 0; i < n; i++)
        {
            cin >> dis >> coin;

        }
    }
}
