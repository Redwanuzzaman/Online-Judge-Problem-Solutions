#include <bits/stdc++.h>
using namespace std;
#define sft scanf("%lld",&t)
#define sfx scanf("%lld",&x)

int main()
{
    int t, n;
    sft;
    while(t--)
    {
        sfn;
        string s;
        cin >> s;
        int len = s.size() / 2;

        for(int k = 1; k <= len; k++)
        {
            string tmp = "";
            for(int i = 0; i < k; i++)
            {
                tmp += s[i];
            }
        }
    }
}
